# 我如何從零開始打造自己的待辦事項 App（你也可以！）

> 一份適合新手的全端任務管理 Web 應用開發指南 —— 不需要資工學位也能看懂。

## 🤔 為什麼要自己動手做？

你可能用過 Microsoft To Do、Todoist 或 Apple 提醒事項。它們都很好用 —— 但如果你能自己做一個呢？不只是為了好玩，而是藉此**真正理解現代 Web 應用程式的運作原理**。

這正是我做的事。在這篇文章中，我會帶你走一遍我如何從零打造 **MyTODO** 的完整過程，並用最淺顯的方式解釋每個概念。

---

## 📸 它長什麼樣子？

MyTODO 看起來和用起來都像一個真正的生產力工具：

- 📋 **多清單管理**（工作、個人、購物等）—— 就像 Microsoft To Do
- ✅ **完整任務管理** —— 新增、編輯、完成、刪除任務
- 🎯 **優先級設定** —— 高、中、低
- 📅 **截止日期與時間**
- 🖱️ **拖拽操作** —— 用滑鼠拖動任務到不同清單
- 📱 **手機友好** —— 在任何螢幕尺寸上都能正常使用
- 🔔 **智慧提醒** —— 任務到期前透過 Telegram 或 Email 通知你
- 🔐 **使用者帳號** —— 帳號密碼登入，資料完全私密

---

## 🧱 建構基石（別擔心，我會一一解釋）

在開始之前，讓我先解釋關鍵技術。把開發 Web 應用想像成蓋房子：

### 前端（Frontend）—— 「門面」（使用者看到的東西）

| 技術 | 它做什麼 | 可以想成... |
|---|---|---|
| **React** | 建構互動式 UI 元件 | 牆壁、窗戶和門 |
| **Vite** | 快速開發工具 | 讓施工更快的電動工具 |
| **Tailwind CSS** | 不用寫太多 CSS 就能讓介面變好看 | 盒裝室內設計方案 |
| **@dnd-kit** | 拖放功能 | 可以重新排列的家具 |

### 後端（Backend）—— 「引擎室」（幕後運作）

| 技術 | 它做什麼 | 可以想成... |
|---|---|---|
| **Node.js** | 在伺服器上執行 JavaScript | 驅動一切的電力 |
| **Express** | 處理網路請求 | 把請求路由到正確地方的櫃台人員 |
| **SQLite** | 把資料存在一個檔案裡 | 一個檔案櫃 |
| **JWT** | 安全的使用者認證 | 活動上的 VIP 手環 |

### 部署 —— 把應用放上網際網路

| 技術 | 它做什麼 |
|---|---|
| **Linux (WSL2)** | 伺服器環境 |
| **Systemd** | 讓應用 24 小時運行 |
| **Cloudflare Tunnel** | 不用開放路由器端口就能獲得公開 URL |
| **HTTPS** | 加密使用者與伺服器之間的資料傳輸 |

---

## 🏗️ 應用如何運作（架構簡化版）

```
┌─────────────┐         ┌─────────────┐         ┌──────────┐
│   瀏覽器     │ ──WiFi──▶│   伺服器     │ ──讀取──▶│  SQLite  │
│  （前端）    │◀──WiFi──│  （後端）    │◀─寫入──│  資料庫  │
└─────────────┘         └──────┬──────┘         └──────────┘
                               │
                    ┌──────────┼──────────┐
                    ▼                     ▼
              ┌──────────┐         ┌──────────┐
              │ Telegram  │         │  Email   │
              │  （機器人）│         │ （SMTP） │
              └──────────┘         └──────────┘
```

**當你打開應用時，發生了什麼事：**

1. 瀏覽器載入 React 前端（好看的介面）
2. 當你新增任務時，React 發送訊息給 Express 伺服器
3. 伺服器檢查你的 JWT token（你登入了嗎？）
4. 伺服器將任務儲存到 SQLite 資料庫
5. 任務到期時，伺服器透過 Telegram 或 Email 發送提醒

---

## 📂 專案結構（各個檔案負責什麼）

```
MyTODO/
├── server/                  # 後端（大腦）
│   ├── index.js             # 主入口 — 啟動伺服器
│   ├── db.js                # 資料庫設定 — 建立資料表
│   ├── routes/
│   │   ├── auth.js          # 登入/註冊邏輯
│   │   ├── lists.js         # 清單管理（增刪改查）
│   │   └── tasks.js         # 任務管理（增刪改查）
│   ├── services/
│   │   └── notifier.js      # Telegram + Email 通知
│   ├── middleware/
│   │   └── auth.js          # JWT token 驗證
│   └── tests/               # 自動化測試（覆蓋率 94.69%！）
│
├── client/                  # 前端（門面）
│   └── src/
│       ├── App.jsx          # 主應用元件
│       ├── api/index.js     # 與後端通訊
│       ├── components/
│       │   ├── Dashboard.jsx    # 主畫面（清單與任務）
│       │   ├── Login.jsx        # 登入/註冊頁面
│       │   ├── TaskModal.jsx    # 任務新增/編輯彈窗
│       │   └── Settings.jsx     # 使用者設定
│       ├── hooks/index.js   # 可重複使用的邏輯（自定義 React hooks）
│       ├── theme.jsx        # 深色/淺色主題
│       └── i18n.jsx         # 多語言支援（英文/中文）
│
├── deploy.sh                # 一鍵部署腳本
└── mytodo.service           # Systemd 服務（開機自動啟動）
```

---

## 🔑 關鍵概念解釋

### 1. 客戶端-伺服器模型（Client-Server Model）
想像一下餐廳：
- **客戶端（瀏覽器）** = 你，看菜單並點餐
- **伺服器（後端）** = 廚房，準備你的食物
- **API** = 服務生，在你和廚房之間傳遞訂單

### 2. REST API
「服務生」使用一種標準語言叫做 REST：
- `GET /api/tasks` → 「給我看所有任務」
- `POST /api/tasks` → 「新增一個任務」
- `PUT /api/tasks/123` → 「更新第 123 號任務」
- `DELETE /api/tasks/123` → 「刪除第 123 號任務」

### 3. JWT 認證
登入時，伺服器給你一個 **token**（就像夜店的 VIP 手環）。之後每次發請求，你都要出示這個 token。伺服器驗證後說：「OK，你可以進來。」

### 4. 資料庫（SQLite）
跟大型資料庫（MySQL、PostgreSQL）不同，SQLite 把所有東西存在**一個檔案**裡（`mytodo.db`）。對個人專案來說簡直完美 —— 不需要額外的資料庫伺服器！

---

## 🧪 測試：為什麼 94.69% 的覆蓋率很重要

我寫了 **83 個自動化測試**，幾乎涵蓋了每一行程式碼。為什麼？

- **信心保證**：如果我改了某個東西導致測試失敗，我能立刻發現
- **活的文件**：測試精確展示了程式碼應有的行為
- **專業實踐**：在真正的公司裡，測試是必須的

測試範例：
```javascript
test('應該能建立新任務', async () => {
  const response = await request(app)
    .post('/api/tasks')
    .set('Authorization', `Bearer ${token}`)
    .send({
      title: '買菜',
      list_id: listId,
      priority: 'high',
      due_date: '2026-04-15'
    });
  
  expect(response.status).toBe(201);
  expect(response.body.title).toBe('買菜');
});
```

---

## 🖥️ 開發環境

這整個專案是透過與運行在 [OpenClaw](https://github.com/openclaw/openclaw) 上的 **AI 助手**對話建構的。以下是完整的開發環境：

| 項目 | 詳情 |
|---|---|
| **主機系統** | Windows 11 + WSL2 (Ubuntu 24.04) |
| **AI 平台** | OpenClaw 2026.3.28 → 2026.4.10 |
| **AI 模型** | GLM-4（透過 z.ai）+ 本地 Gemma 模型 |
| **通訊方式** | Telegram（透過聊天讓 AI 寫程式碼） |
| **編輯器** | 無 —— 所有程式碼都由 AI 助手撰寫 |
| **部署目標** | AppServer WSL (Ubuntu, IP: 172.31.254.165) |
| **公開域名** | `first.pet`（透過 Cloudflare Tunnel） |

**這代表什麼？** 我沒有打開 VS Code 或任何 IDE。我只用日常語言在 Telegram 上描述我想要的東西，AI 就幫我寫好、測試好、部署好程式碼。如果你能描述你想要什麼，你也能做到。

---

## 💬 我使用的 Prompt（逐步重現指南）

以下是我發送給 AI 助手的實際 prompt，從零到上線的完整過程。你可以用任何有能力寫程式的 AI 助手，跟著這些 prompt 重現整個專案。

### 階段一：基礎建設

**Prompt 1 — 初始需求：**
> 我想做一個類似 Microsoft To Do 的待辦事項 Web 應用。技術棧：
> - 後端：Node.js + Express + SQLite + JWT 認證
> - 前端：React + Vite + Tailwind CSS
> - 通知：Telegram Bot + Email（Nodemailer）
> 
> 需要的功能：
> - 使用者註冊和登入
> - 多個任務清單（如工作、個人、購物）
> - 任務增刪改查，含標題、描述、優先級（高/中/低）、截止日期時間
> - 拖拽排序
> - Telegram 和 Email 到期提醒
> 
> 先建立專案結構並實作後端。

**Prompt 2 — 前端設定：**
> 現在建立 React 前端：
> - 現代風格的登入/註冊頁面
> - 側邊欄顯示所有清單的儀表板
> - 可在清單之間拖拽的任務卡片
> - 新增/編輯任務的彈窗，包含所有欄位
> - 使用 Tailwind CSS 樣式，預設深色主題

**Prompt 3 — 前後端串接：**
> 建立前端的 API 層連接後端：
> - 認證 API（登入、註冊、token 管理）
> - 清單 API（增刪改查）
> - 任務 API（增刪改查）
> - 在 localStorage 處理 JWT token
> - 錯誤處理和載入狀態

### 階段二：打磨與功能

**Prompt 4 — UI 增強：**
> 加入這些 UI 改進：
> - 側邊欄可折疊（切換按鈕）
> - 每個清單分配隨機彩色圖標（30 種不重複）
> - 新增「所有任務」視圖，顯示所有清單的任務
> - 新增「今天」篩選器，顯示今天到期的任務
> - 新增「已完成」區域
> - 拖拽任務到不同清單時，應該更改其 list_id

**Prompt 5 — 多語言支援：**
> 為應用加入國際化（i18n）：
> - 支援英文和繁體中文（zh-TW）
> - 在設定中加入語言切換
> - 為所有 UI 文字建立語言檔

### 階段三：測試與品質

**Prompt 6 — 自動化測試：**
> 為後端撰寫全面的測試：
> - 測試所有認證路由（註冊、登入、無效憑證）
> - 測試清單和任務的所有增刪改查操作
> - 測試授權（使用者只能存取自己的資料）
> - 測試邊界情況（空白欄位、重複名稱、無效 ID）
> - 使用 Jest + Supertest
> - 目標覆蓋率 90% 以上

### 階段四：部署

**Prompt 7 — 部署腳本：**
> 建立部署腳本：
> - 安裝所有依賴（前後端都執行 npm install）
> - 建置 React 前端用於正式環境
> - 建立 systemd 服務檔，讓應用開機自動啟動
> - 伺服器在正式環境模式下提供建置好的前端
> - 使用 port 3001

**Prompt 8 — Cloudflare Tunnel：**
> 幫我設定 Cloudflare Tunnel 讓應用公開存取：
> - 我在 Cloudflare 上有域名 `first.pet`
> - 我想要子域名 `mytodo.first.pet`
> - 設定命名隧道（不是快速隧道）
> - 為隧道建立 systemd 服務讓它自動啟動

### 階段五：迭代

**Prompt 9 — 錯誤修復與調整：**
> 修復這些問題：
> - 拖拽任務到另一個清單後，應該立即從舊清單消失（不要等到重新整理）
> - 每個清單的任務計數應該即時更新
> - 刪除清單前加入確認對話框

### 🔄 Prompt 撰寫技巧

根據我的經驗，好的軟體開發 prompt 應該：

1. **明確指定技術棧** —— 不要說「做一個 Web App」。要說「使用 React + Express + SQLite」。
2. **清楚列出功能** —— 用項目符號一一列舉
3. **分階段建構** —— 不要一次要求所有東西。後端 → 前端 → 整合 → 打磨 → 部署。
4. **持續迭代** —— 看到結果後再優化。「修復這個 bug」或「加這個功能」完全沒問題。
5. **邊做邊測試** —— 早點請 AI 寫測試，在問題變大之前就能發現。

---

## 🤖 AI 實際做了什麼

對於每個 prompt，AI 助手做了以下事情：
1. 建立必要的檔案並撰寫所有程式碼
2. 執行 `npm install` 安裝依賴
3. 執行測試並回報覆蓋率
4. 透過 SSH 部署到伺服器
5. 設定 systemd 服務確保 24 小時運行
6. 設定 Cloudflare Tunnel 實現公開存取

**我從未打開程式碼編輯器。我從未親手寫過一行程式碼。** 我只是描述我想要的東西，AI 就幫我實現了。

---

## 🌐 我如何把它部署到網際網路上

### 問題
我的應用跑在家用伺服器上。如何讓任何人都能存取，又不用花錢租雲端主機？

### 解決方案：Cloudflare Tunnel
```
你的手機 ──▶ Cloudflare CDN ──▶ 隧道 ──▶ 你的家用伺服器
              (mytodo.first.pet)           （在 NAT/路由器後面）
```

Cloudflare Tunnel 從我的伺服器到 Cloudflare 的全球網路建立一條安全連線。不需要：
- 在路由器上開放端口（安全風險！）
- 購買固定 IP 地址
- 手動設定 SSL 憑證

### 步驟
1. 在伺服器上安裝 `cloudflared`
2. 建立隧道：`cloudflared tunnel create mytodo`
3. 設定指向 `localhost:3001`
4. 指定域名：`mytodo.first.pet`
5. 設定 systemd 服務讓它開機自動啟動
6. 完成！應用現在可以透過 HTTPS 從全球存取 🎉

---

## 💡 學到的經驗

### 做得好的地方
- **從簡單開始，逐步迭代**：我沒有試圖一次建好所有東西。先是基本的增刪改查，然後是拖拽功能，再來是通知系統。
- **邊寫邊測試**：跟著程式碼一起寫測試，省下了無數的除錯時間
- **SQLite 被低估了**：對個人專案來說，它就夠用了

### 如果重來會做得不同的地方
- **更早規劃資料庫結構**：新增功能時我不得不遷移資料
- **手機優先設計**：我先做了桌面版，後來才修手機版
- **錯誤處理**：我應該從一開始就思考「什麼地方會出錯」

---

## 🚀 你也可以試試

如果你想做類似的東西，以下是我的推薦學習路徑：

1. **學 HTML/CSS/JavaScript 基礎**（免費：[MDN Web Docs](https://developer.mozilla.org)）
2. **學 React**（免費：[React 官方教學](https://react.dev/learn)）
3. **學 Node.js + Express**（免費：[The Odin Project](https://www.theodinproject.com)）
4. **做一個簡單的增刪改查應用**（待辦清單就是最好的練習！）
5. **一次加一個功能**（認證、通知等）
6. **部署上線**（Cloudflare Tunnel 免費且對新手友善）

---

## 📊 專案數據

| 指標 | 數值 |
|---|---|
| 程式碼總行數 | ~3,000+ |
| 測試覆蓋率 | 94.69% |
| 測試數量 | 83 |
| 技術棧 | React, Node.js, Express, SQLite |
| 部署方式 | 自架伺服器 + Cloudflare Tunnel |
| 公開網址 | https://mytodo.first.pet/ |

---

## 🙌 結語

開發這個應用讓我學到的比任何教程都多。每天使用自己親手打造的工具，那種感覺是很神奇的。

最棒的是？**這一切不需要資工學位。** 所有資源都是免費的，工具都是開源的，你唯一需要投入的是時間和好奇心。

如果一個新手能做出這些，你也可以。從小處著手，持續建造，不要害怕搞壞東西 —— 這就是學習的方式！💪

---

*由 [Denvy Shi](https://github.com/Denvy-Shi) 用 ❤️ 製作*
*原始碼：[GitHub](https://github.com/Denvy-Shi/MyTODO)*
